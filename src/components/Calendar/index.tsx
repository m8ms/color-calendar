import React from "react";
import styled from "styled-components";

const CalendarBody = styled.section`
  padding: 10px;
`;

const Calendar: React.FC = () => (
  <CalendarBody>This is thy calendar</CalendarBody>
);

export default Calendar;
